# Settings for the niri shell, shipped as ~/.config/quickshell/settings.json
# (generated by modules/home/dotfiles/default.nix). Edit here and rebuild;
# the shell has no runtime settings UI on purpose.
{
  appearance = {
    accent = "#3daee9";        # Breeze blue
    accentFromWallpaper = false;   # true: derive the accent from the wallpaper instead
    panelOpacity = 0.95;       # 0..1, bar and popups
    barHeight = 36;
    radius = 4;
    font = "JetBrainsMono Nerd Font";
    fontSize = 10;
  };

  bar = {
    # right-side widgets, in display order; set to false to hide
    tray = true;
    layout = true;
    volume = true;
    bluetooth = true;
    battery = true;
    cpu = true;
    memory = true;
    temps = true;
    disk = true;
    network = true;
    notifications = true;
    control = true;
    sidebar = true;
    # centre
    nowPlaying = true;
    windowTitle = true;        # focused window icon + title next to the workspaces
    workspaceIcons = true;     # app icons inside the workspace pills
    marqueeWidth = 220;
    clockFormat = "HH:mm";
    dateFormat = "ddd d MMM";
  };

  # desktop ids shown in the sidebar's quick launch
  pinned = [
    "org.kde.dolphin" "zen-beta" "code" "dev.zed.Zed" "discord"
    "moe.kopuz.kopuz" "com.mitchellh.ghostty" "steam" "thunderbird" "org.jellyfin.JellyfinDesktop"
  ];

  weather = { city = "Istanbul"; };

  dashboard = { visualizer = true; };   # cava bars in the media card (runs only while it is open)

  osd = { timeoutMs = 1500; bottomMargin = 90; };

  notifications = { timeoutMs = 6000; maxPopups = 5; };

  wallpaper = "";              # empty = the one shipped in the repo
}
